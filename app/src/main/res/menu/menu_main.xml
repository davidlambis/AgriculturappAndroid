<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    tools:showIn="navigation_view">


    <!--<group android:checkableBehavior="single">
      <item
        android:id="@+id/nav_camera"
        android:iconTint="@color/white"
        android:icon="@drawable/ic_cloud_done"
        android:title="Import" />
    </group>-->

    <item android:title="Menu" >
        <menu>
            <item

                android:id="@+id/nav_proveedores"
                android:icon="@drawable/ic_ic_proveedores"
                android:title="Mis Proveedores" />

            <item
                android:id="@+id/nav_ofertas"
                android:icon="@drawable/ic_ic_ofertas_desplegable"
                android:iconTint="@color/white"
                android:title="Ofertas" />

            <item
                android:id="@+id/nav_compras_realizadas"
                android:icon="@drawable/ic_ic_compras_realizadas_desplegable"
                android:iconTint="@color/white"
                android:title="Compras Realizadas" />
            <item
                android:id="@+id/nav_productos"
                android:icon="@drawable/ic_ic_productos_interes_desplegable"
                android:iconTint="@color/white"
                android:title="Productos Interes" />
            <item
                android:id="@+id/nav_preguntas"
                android:icon="@drawable/ic_ic_pregunas_desplegable"
                android:iconTint="@color/white"
                android:title="Preguntas" />
        </menu>
    </item>
</menu>
