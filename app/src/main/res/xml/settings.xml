<?xml version="1.0" encoding="utf-8"?>
<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">
    <CheckBoxPreference
        android:key="pref_notifications"
        android:title="@string/pref_notifications"
        android:defaultValue="true" />

    <!--<PreferenceCategory
        android:title="@string/pref_vibration_title">
        <CheckBoxPreference
            android:key="pref_vibration"
            android:title="@string/pref_vibration"
            android:defaultValue="true" />
        <ListPreference
            android:dependency="pref_vibration"
            android:key="pref_vibration_type"
            android:title="@string/pref_vibration_type"
            android:entries="@array/pref_vibration_type"
            android:defaultValue="@string/pref_vibration_type_default" />
    </PreferenceCategory>-->

</PreferenceScreen>