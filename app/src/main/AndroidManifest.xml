<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.interedes.agriculturappv3">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".asistencia_tecnica.splash.SplashActivity"
            android:screenOrientation="portrait"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".asistencia_tecnica.home.HomeActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".asistencia_tecnica.login.LoginActivity"
            android:configChanges="screenSize|orientation"
            android:theme="@style/LoginTheme"
            android:windowSoftInputMode="stateHidden" />
        <activity
            android:name=".asistencia_tecnica.registration.register_user.RegisterUserActivity"
            android:parentActivityName=".asistencia_tecnica.home.HomeActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".asistencia_tecnica.registration.RegisterProductorActivity"
            android:configChanges="screenSize|orientation"
            android:parentActivityName=".asistencia_tecnica.registration.register_user.RegisterUserActivity"
            android:theme="@style/LoginTheme"
            android:windowSoftInputMode="stateHidden" />

        <activity  android:theme="@style/AppTheme.NoActionBar" android:name=".asistencia_tecnica.modules.MenuMainActivity"></activity>
    </application>

</manifest>